import { AdminShell } from "@/components/AdminShell";import { AppointmentActions } from "@/components/AppointmentActions";import { StatusBadge } from "@/components/StatusBadge";import { requireTenantMembership } from "@/server/authorization";import { prisma } from "@/lib/prisma";import { dateTime } from "@/lib/format";export const dynamic="force-dynamic";
export default async function Appointments(){const {salon,role}=await requireTenantMembership(["SALON_ADMIN","SALON_MANAGER"]);const appointments=await prisma.appointment.findMany({where:{salonId:salon.id},include:{service:true,provider:true},orderBy:{startsAt:"desc"},take:100});const links=role==="SALON_MANAGER"?[{href:"/salonmanager",label:"Təqvim"},{href:"/salonadmin/appointments",label:"Rezervasiyalar"}]:[{href:"/salonadmin",label:"Ümumi baxış"},{href:"/salonadmin/appointments",label:"Rezervasiyalar"},{href:"/salonadmin/services",label:"Xidmətlər"},{href:"/salonadmin/team",label:"Komanda"},{href:"/salonadmin/hours",label:"İş saatları"}];return <AdminShell title={salon.name} links={links}><div className="admin-page-heading"><div><p className="eyebrow">Salon əməliyyatları</p><h1>Rezervasiyalar</h1><p className="muted">Müştəri görüşlərini nəzərdən keçirin və statusunu təhlükəsiz dəyişin.</p></div><span className="appointment-count">{appointments.length} rezervasiya</span></div><div className="table-wrap appointment-table"><table><thead><tr><th>Müştəri</th><th>Vaxt</th><th>Xidmət</th><th>Status</th><th>İdarəetmə</th></tr></thead><tbody>{appointments.map(a=><tr key={a.id}><td data-label="Müştəri"><strong>{a.customerName}</strong><br/><span className="muted small">{a.customerEmail}</span></td><td data-label="Vaxt">{dateTime(a.startsAt)}</td><td data-label="Xidmət">{a.service.name} <span className="muted">·</span> {a.provider.name}</td><td data-label="Status"><StatusBadge status={a.status} /></td><td data-label="İdarəetmə"><AppointmentActions appointmentId={a.id} currentStatus={a.status}/></td></tr>)}</tbody></table></div></AdminShell>}
